# mdread E2E sample

This document is opened through the operating system startup argument.

## Keyboard navigation

The automated test verifies the document appears after startup.

## Menu

The test also verifies reader menu entries remain available.

## 目录回归测试章节 01：这是一个用于验证右侧目录文本截断、宽度约束和省略号显示的超长标题内容

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 02

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 03

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 04

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 05

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 06

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 07

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 08

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 09

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 10

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 11

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 12

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 13

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 14

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 15

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 16

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 17

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 18

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 19

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 20

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 21

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 22

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 23

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 24

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 25

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 26

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 27

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 28

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 29

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 30

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 31

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 32

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 33

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 34

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 35

用于验证目录面板的垂直滚动和横向文本边界。

## 目录回归测试章节 36

用于验证目录面板的垂直滚动和横向文本边界。

```ts
console.log('mdread e2e');
```
