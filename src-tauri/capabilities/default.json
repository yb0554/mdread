{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "mdread 只读权限 — 仅允许对话框选择, 不授予任何文件写入权限",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "core:webview:allow-print",
    "opener:default",
    "core:window:allow-set-title"
  ]
}
